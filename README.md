# House-Prediction-Model
A machine learning–based regression project that predicts median house values using structured housing data. The project follows a clean ML workflow with stratified sampling, feature preprocessing pipelines, and model persistence for production-ready inference.
